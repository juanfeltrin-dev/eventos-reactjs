import React, { useState } from 'react';

function Menu() {
    const [sideBar, setSideBar] = useState(false);
    
    return(
        <>
            <div id="sidebar">
            teste
            </div>
        </>
    );
}

export default Menu;